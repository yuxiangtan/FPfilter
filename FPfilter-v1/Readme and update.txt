Useless steps are skipped (Step 1 and 2 in v0.2)
PT bed will not be considered in this version anymore.

This is the version on anaconda, in which rtg should be activated in the  environment already.

Final outputs:
${input_vcf}_TP_after_filtered_sorted.vcf: the "TP" locations which passed the filter.
${input_vcf}_filtered_FP_sorted.vcf :the "false positive" locations which were filtered